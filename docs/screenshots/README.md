# UI screenshots

PNG files in this folder are used by the root [README](../../README.md).

To refresh after UI changes, run [scripts/capture_screenshots.py](../../scripts/capture_screenshots.py) against a running BFIntel instance. Pass credentials via environment variables only — do not store them in this repo.
